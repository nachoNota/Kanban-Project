@{
    ViewData["Title"] = "No encontrado";
    var mensaje = ViewData["Mensaje"] as string ?? "El recurso solicitado no fue encontrado.";
}

<div class="text-center mt-5">
    <h2 class="text-danger">Recurso no encontrado</h2>
    <p>@mensaje</p>
    <a href="javascript:history.back()" class="btn btn-primary">Volver atrás</a>
</div>
